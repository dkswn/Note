노드란? 
- 네트워크에 연결된 모든 물리적인 기기 혹은 장치

라우팅이란?
- 네트워크에서 경로를 선택하는 프로세스
- 보통 노드와 노드간에 통신을 하려고 할때, 통신하는 경로의 가짓수는 여러개이다 이때 여러 경로 중 최적의 경로를 선택하여 통신을 하는데 이것을 라우팅이라고 한다.

객체란?
-변수.

뱃지
-알람,필터버튼,장바구니.

페이지네이션
- 페이지들이 나열된 것.

픽커
세로로된 날짜 컴포넌트.

HTML 
- 하이퍼텍스트 마크업 랭귀지 줄임말.
- 웹 콘텐츠의 구조와 의미를 정의하고 부여하는 마크업 언어.
- 태그 등을 이용해 문서나 데이터의 구조를 표시한 언어.

DOM 
- 문서객체모델
- 웹페이지를 파싱하고, 해석하는 전체모델을 지칭함.

CSS란?
- 태그들에게 스타일 효과를 주는 언어.
- 선택자: 효과를 부여하는 고유한 문법(태그선택자,id선택자,class 선택자).

margin: 바깥쪽 여백/ 부모와 떨어져 있는 용도.
padding: 안쪽 여백/ 자신과 자식요소 사이의 영역.

서버와 클라이언트

서비스를 제공하느냐/ 제공 받느냐에 따른 구분.

화면을 비롯한 클라이언트 영역을 프론트엔드라고한다.
서버의 영역을 관리 개발하는 개발을 백앤드라고 한다.

플러그인
- 어떠한 특정한 기능을 해결하는데 쓸 수 있또록 미리 만들어 놓은 코드 및 데이터.

웹 접근성
- 누구에게나 평등한 웹 환경을 구성하는 요소.

검색엔진 최적화(SEO)
 - 웹페이지를 구글 등 검색엔진이나 포털사이트의 기준에 맞춰 구성하는 작업.

트래픽
- 서버를 통해 전송되는 데이터 양 , 데이터의 양, 서비스 방문자 수가 많을 수록 증가하며 너무 높아지면 서버가 먹통이 된다.

DBMS
- 데이터베이스 관리 시스템이라고 하며 데이터를 저장,수정,추출 할 수 있도록 관리하는 프로그램.

관계형 데이터베이스
- 키와 값의 관계로 이루어진 표로 데이터를 구성하는 방식으로 줄여서 RDB라고함, 오라클,MySQL 등 널리 쓰이는 데이터베이스.

쿼리
관계형 데이터베이스에서 데이터를 요청하고 관리하기 위해 사용되는 명령어 또는 질의 그자체.

API(Application Programing interface) 
- 특정한 기능을 사용하기 위해 필요한 데이터를 주고 받게끔 만든 도구나 방법, 중간창구 역활

SDK
- 소프트웨어나 시스템을 만드는데 쓰이는 도구 (샘플코드,코드편집기,콘솔, 안내문서, API)

IDE 
- 프로그램을 개발하는데 필요한 소스코드 작성 및 편집,컴파일,디버깅을 한번에 할수 있는 통합개발환경(비쥬얼스튜디오,인텔리제이,이클립스 등)

유닉스
- 기계어에 가까운 언어, 주로 서버개발이나 시스템 개발에 쓰인다.

스레드
- 프로그램을 실행하는 하나의 프로세스, 작업을 처리하는 주체, 모든 프로세스는 한개이상의 스레드를 가짐

멀티스레드
- 하나의 프로세스가 두개이상의 스레드를 가지고 있는 경우이다. 각각의 스레드가 프로세스를 공유하여 자원을 절약 할 수 있고 스레드 간에 정보교환이 쉽다.

도메인
- 숫자로만 이루어진 IP와 달리 naver.com 처럼 문자로 지정된 인터넷 주소(URL).

DNS(Domain Name System)
- 도메인이 올라가 있는 웹서버의 ip로 연결하는 시스템

동기 ,비동기
- 동기: 기능에 대한 요청을 보내고 응답을 받아야만 동작이 순차적으로 이루어진다.
- 비동기: 요청을 보내는 순서와 상관 없이 동작이 처리된다.

Ajax
- 한 웹페이지를 로딩 할 떄 페이지의 일부 데이터만 갱신하는 웹 개발 기법
- 
